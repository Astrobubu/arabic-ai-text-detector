# Runtime dependencies are defined in pyproject.toml.
# Layer 1 (heuristics) uses only the Python standard library at runtime.
# Layer 2 (local ML classifier), document upload, and the desktop backend
# server are optional extras: pip install -e ".[ml,docs,server]" or ".[all]"
